"use strict";
// QUESTION 31 NO USERS.
let users = ['Azhar', 'Ali', 'Eric', 'Admin', 'Shakeel'];
if (users.length == 0) {
    console.log("We need to find some users!");
}
else {
    users = [];
    console.log("We need to find some users!");
}
